using UnityEngine;
using System.Collections;

public class bouncerTalk : MonoBehaviour {

	public AudioSource bouncer;

	// Use this for initialization
	void Start () {
		bouncer = bouncer;
	
	}
	
	// Update is called once per frame
	void OnMouseEnter() {
		bouncer.Play ();
	
	}
}
